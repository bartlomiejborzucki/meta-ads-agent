[project]
name = "meta-ads-agent"
version = "0.7.0"
description = "Agent-native toolkit for operating Meta Ads through Meta's official Ads MCP, with a narrow Marketing API fallback"
readme = "README.md"
requires-python = ">=3.11"
license = { text = "MIT" }
authors = [{ name = "meta-ads-agent contributors" }]
keywords = ["meta-ads", "facebook-ads", "instagram-ads", "mcp", "agent-skills", "advertising"]
classifiers = [
  "Development Status :: 3 - Alpha",
  "Environment :: Console",
  "Intended Audience :: Developers",
  "License :: OSI Approved :: MIT License",
  "Programming Language :: Python :: 3.11",
  "Programming Language :: Python :: 3.12",
  "Programming Language :: Python :: 3.13",
  "Topic :: Office/Business",
]

# Deliberately minimal. Validation and config parsing only. No HTTP client, no
# web framework, no database - see docs/architecture/overview.md.
dependencies = [
  "pydantic>=2.7,<3",
  "PyYAML>=6.0,<7",
]

[project.optional-dependencies]
# The Marketing API fallback. Not installed by default: a user who only needs
# capabilities the official MCP covers never downloads this.
# Majors track Graph API versions, so the range is pinned to one major.
api = ["facebook-business>=26.0,<27"]
dev = [
  "pytest>=8.0",
  "pytest-cov>=5.0",
  "ruff>=0.6",
  "mypy>=1.11",
  "types-PyYAML>=6.0",
]

[project.scripts]
meta-ads-agent = "meta_ads_agent.cli.main:main"

[project.urls]
Homepage = "https://github.com/bartlomiejborzucki/meta-ads-agent"
Documentation = "https://github.com/bartlomiejborzucki/meta-ads-agent/tree/master/docs"
Issues = "https://github.com/bartlomiejborzucki/meta-ads-agent/issues"
Changelog = "https://github.com/bartlomiejborzucki/meta-ads-agent/blob/master/CHANGELOG.md"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/meta_ads_agent"]

# Data files the CLI reads at runtime. There is one copy of each in the
# repository and the build puts it where each consumer looks: the workspace
# templates live inside the meta-ads-core skill so a standalone skill install
# still has them, and are copied into the wheel so an installed CLI is
# self-sufficient. See docs/reference/packaging.md.
[tool.hatch.build.targets.wheel.force-include]
"config/capabilities.yaml" = "meta_ads_agent/_data/capabilities.yaml"
"config/mcp-tools.yaml" = "meta_ads_agent/_data/mcp-tools.yaml"
"skills/meta-ads-core/assets" = "meta_ads_agent/_data/templates"
# The skills ship inside the wheel as well as in the repository. That is what
# makes one install command possible: `uv tool install git+...` puts the CLI
# on the machine and the CLI already has the payload it is about to copy, so
# the two can never be different versions of the project.
"skills" = "meta_ads_agent/_data/skills"
"release-manifest.json" = "meta_ads_agent/_data/release-manifest.json"

[tool.hatch.build.targets.sdist]
include = [
  "src/", "config/", "skills/", "integrations/", "packaging/",
  "release-manifest.json",
  "docs/", "examples/", "tests/",
  "README.md", "LICENSE", "CHANGELOG.md", "THIRD_PARTY_NOTICES.md",
  "upstreams.yaml", ".env.example",
]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-q --strict-markers"
markers = [
  # Live tests are excluded by default. They require META_ADS_LIVE_TESTS=1 and a
  # designated test ad account. They never run in CI. See tests/live/README.md.
  "live: talks to a real Meta account (opt-in, never in CI)",
]

[tool.ruff]
line-length = 100
target-version = "py311"
src = ["src", "tests", "scripts"]

[tool.ruff.lint]
select = ["E", "F", "W", "I", "N", "UP", "B", "SIM", "RUF", "S"]
ignore = [
  "S101",    # assert is fine in tests, and ruff's per-file ignores cover src
  "RUF012",  # ClassVar annotations on pydantic models are noise
  # Exception names read better without an Error suffix here, and DryRun is
  # deliberately not an error - it signals a completed preview.
  "N818",
]

[tool.ruff.lint.per-file-ignores]
# Fake tokens and temp paths in fixtures, and `git`/`python` resolved from PATH -
# these run in a developer checkout or CI, never in a user's environment.
"tests/**" = ["S105", "S106", "S108", "S607"]
"scripts/**" = ["S607"]

[tool.mypy]
python_version = "3.11"
packages = ["meta_ads_agent"]
mypy_path = "src"
strict = true
warn_unreachable = true
# facebook-business ships no type information.
[[tool.mypy.overrides]]
module = ["facebook_business.*"]
ignore_missing_imports = true
